import { ReactNode } from 'react';

export interface HTTPHeader {
    POST: {
        color: string;
        background: string;
        border: string;
    },
    GET: {
        color: string;
        background: string;
        border: string;
    }
}